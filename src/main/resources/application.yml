spring:
  application:
    name: hello-spring-cloud-zuul

server:
  port: 8770

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

zuul:
  routes:
    api-a:
      path: /api/a/**
      serviceId: itoken-web-admin-feign