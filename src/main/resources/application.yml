spring:
  application:
    name: itoken-zuul

  zipkin:
        base-url: http://localhost:8780

server:
  port: 8770

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

zuul:
  routes:
    api-a:
      path: /api/a/**
      serviceId: itoken-web-admin-feign